export type FilterOptions = {
    startDate?: Date,
    endDate?: Date,
    walletId?: string,
    categoryId?: string,
}

export type TransactionFilter = {
    startDate?: Date;
    endDate?: Date;
    walletId?: string;
    categoryId?: string;
}